-- ==========================================================
-- GovTech GSRP Backend Assessment - MySQL Schema
-- Synchronized with the current JPA model
-- ==========================================================

CREATE DATABASE IF NOT EXISTS gsrp_db;
USE gsrp_db;

-- 1. Users and Roles
CREATE TABLE IF NOT EXISTS users (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE
) ENGINE=InnoDB;

CREATE TABLE IF NOT EXISTS user_roles (
    user_id BIGINT NOT NULL,
    role VARCHAR(255),
    PRIMARY KEY (user_id, role),
    CONSTRAINT fk_user_roles_user FOREIGN KEY (user_id)
        REFERENCES users(id) ON DELETE CASCADE
) ENGINE=InnoDB;

-- 2. Citizens
CREATE TABLE IF NOT EXISTS citizens (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    nic VARCHAR(20) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL UNIQUE,
    mobile VARCHAR(15) NOT NULL,
    address VARCHAR(255) NOT NULL,
    status VARCHAR(50) NOT NULL
) ENGINE=InnoDB;

-- 3. Service Requests
CREATE TABLE IF NOT EXISTS service_requests (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    citizen_id BIGINT NOT NULL,
    service_type VARCHAR(50) NOT NULL,
    description VARCHAR(1000) NOT NULL,
    status VARCHAR(50) NOT NULL,
    created_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_service_request_citizen FOREIGN KEY (citizen_id)
        REFERENCES citizens(id) ON DELETE CASCADE
) ENGINE=InnoDB;

-- 4. Supporting Documents
CREATE TABLE IF NOT EXISTS supporting_documents (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    service_request_id BIGINT NOT NULL,
    type VARCHAR(50) NOT NULL,
    name VARCHAR(255) NOT NULL,
    document_reference VARCHAR(255) NOT NULL,
    verification_status VARCHAR(50) NOT NULL,
    CONSTRAINT fk_supporting_document_request FOREIGN KEY (service_request_id)
        REFERENCES service_requests(id) ON DELETE CASCADE
) ENGINE=InnoDB;

-- 5. Notifications
CREATE TABLE IF NOT EXISTS notifications (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    citizen_id BIGINT NOT NULL,
    service_request_id BIGINT NOT NULL,
    message VARCHAR(500) NOT NULL,
    status VARCHAR(50) NOT NULL,
    created_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_notification_citizen FOREIGN KEY (citizen_id)
        REFERENCES citizens(id) ON DELETE CASCADE,
    CONSTRAINT fk_notification_service_request FOREIGN KEY (service_request_id)
        REFERENCES service_requests(id) ON DELETE CASCADE
) ENGINE=InnoDB;

-- 6. Service Request Status History
CREATE TABLE IF NOT EXISTS service_request_status_history (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    service_request_id BIGINT NOT NULL,
    previous_status VARCHAR(50),
    new_status VARCHAR(50) NOT NULL,
    changed_by VARCHAR(100) NOT NULL,
    changed_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT fk_status_history_request FOREIGN KEY (service_request_id)
        REFERENCES service_requests(id) ON DELETE CASCADE
) ENGINE=InnoDB;

-- ==========================================================
-- Helpful Verification Queries
-- ==========================================================

-- SHOW TABLES;
-- SELECT * FROM users;
-- SELECT * FROM citizens;
-- SELECT * FROM service_requests ORDER BY created_date DESC;
-- SELECT * FROM supporting_documents WHERE service_request_id = 1;
-- SELECT * FROM notifications WHERE citizen_id = 1 ORDER BY created_date DESC;
-- SELECT * FROM service_request_status_history WHERE service_request_id = 1 ORDER BY changed_at ASC;
